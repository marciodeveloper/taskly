# Base URL of the Laravel API, as reached from the browser.
# This is the default used when the variable is absent, so a local run
# without this file still works.
NEXT_PUBLIC_API_URL=http://127.0.0.1:18000
